apiVersion: v1
kind: Service
metadata:
  name: online-features
  namespace: data
spec:
  type: NodePort
  selector:
    app: online-features
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30800
